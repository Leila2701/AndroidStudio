<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.udemyandroid.semana_03">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_ucv"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_ucv_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Semana03">
        <activity android:name=".PromedioNotas"></activity>
        <activity android:name=".IntentsImplicito" />
        <activity android:name=".MainActivity_Fc_Resultado" />
        <activity android:name=".Formulario_Covid" />
        <activity android:name=".MainActivityCalculadora" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>